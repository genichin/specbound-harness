---
id: ms-0001-003
kind: manual-bootstrap-micro-spec
status: active
requirement:
  id: req-0001
  revision: 1
  path: docs/requirements/req-0001/req-0001-r1.md
  sha256: f8c8edb5eeb3a854c1cf3410ed23adda227b03d38235067b2d45069117a4d971
selected_acceptance_criteria: [AC-003]
authority: accountable-user-direct-instruction
manual_bootstrap_reason: Canonical iteration-QC evidence semantics are not yet adopted; this bounded record is implementation and QC evidence for AC-003 only.
---

# MS-0001-003 — Iteration-QC evidence contract

## Objective

Satisfy **REQ-0001-r1 AC-003** by making one canonical iteration-QC record a fail-closed machine evidence record bound to the exact canonical Micro-SPEC snapshot it evaluates.

## Scope

- Extend iteration-QC v1 validation beyond the topology/envelope from AC-001.
- Require canonical Micro-SPEC path, ID, and SHA-256 binding; require the Micro-SPEC to be valid under the existing canonical semantic contract.
- Require the iteration-QC selected AC set to exactly preserve the bound Micro-SPEC selected AC set.
- Require focused reproducible verification entries with command, result, and exit code; permit `verified` only with complete passing evidence.
- Require a bounded verdict and an explicit remaining/unfulfilled AC list derived from the bound parent REQ.
- Add valid and copied-fixture negative coverage for stale Micro-SPEC digest, AC mismatch/out-of-scope coverage, malformed verdict/results, missing focused evidence, and invalid remaining-AC enumeration.
- Update the iteration-QC template, JSON schema, README, governance guide, and repository skill to state the evidence and non-authority contract.

## Non-goals

- Do not implement delivery-QC aggregation, delivery authority, merge, release, adoption enforcement, or end-to-end adopted closure (AC-004 through AC-006).
- Do not issue, rewrite, or relabel approval, REQ, manual-bootstrap, or delivery artifacts.
- Do not treat an iteration-QC record as authorization for approval, merge, delivery, or release.

## Baseline

AC-001 enforces iteration-QC topology and JSON `schema_version: 1`. AC-002 enforces canonical Micro-SPEC parent binding, AC subset validity, and planning completeness. Existing root artifacts remain manual-bootstrap and deliberately non-canonical, so isolated copied fixtures prove the new canonical semantic contract.

## Verification plan

Run from the repository root:

```bash
.venv/bin/python -m pytest -q tests/test_artifact_topology.py
.venv/bin/python -m pytest -q
.venv/bin/python -m specbound.cli preflight
.venv/bin/python -m specbound.cli validate
.venv/bin/python -m compileall -q src
.venv/bin/python -m json.tool schemas/iteration-qc.schema.json >/dev/null
git diff --check
```

## QC exit rule

Record `verified` only when focused and full tests, root CLI checks, source/schema checks, documentation/skill alignment, and diff checks pass. Otherwise record `not_verified` or `blocked` with the failing command. This manual verdict is not a canonical iteration-QC record and cannot contribute delivery coverage.

## Rollback and containment

This high-risk-parent slice changes only repository-local validator logic, isolated fixtures, template/schema, and guidance. If the evidence contract accepts an unsafe/stale claim or rejects valid evidence, stop relying on the AC-003 coverage claim, retain the reproducer, and revert only AC-003-attributable changes. No merge, delivery, release, or external/runtime action is authorized.

## Manual QC verdict

**Verified — 2026-07-22.** Focused topology regression, full suite, root preflight/validate, source compilation, iteration-QC schema parse, and whitespace/diff checks passed. This is manual-bootstrap implementation evidence for AC-003 only; it is not a canonical iteration-QC record and cannot authorize delivery, merge, or release.
