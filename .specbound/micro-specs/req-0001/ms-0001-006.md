---
id: ms-0001-006
kind: manual-bootstrap-micro-spec
status: active
requirement:
  id: req-0001
  revision: 1
  path: .specbound/requirements/req-0001/req-0001-r1.md
  sha256: de286dbc9e12e686df5e628b325020a40206199d78bf36752dbc58754ad47695
selected_acceptance_criteria: [AC-006]
authority: accountable-user-direct-instruction
manual_bootstrap_reason: Canonical issuance is not implemented; this bounded record authorizes only AC-006 isolated fixture/CLI proof and regression coverage.
---

# MS-0001-006 — Executable adopted evidence matrix

## Objective

Satisfy **REQ-0001-r1 AC-006** with isolated copied-fixture tests that exercise the real `specbound.cli` through a complete adopted happy path and assert fail-closed outcomes for every required negative artifact and claim boundary.

## Scope

- Add one real-CLI copied-fixture happy-path test. Its fixture setup writes the three canonical-family files for the exact approved fixture REQ snapshot; the CLI then proves `preflight`, root `validate`, adopted iteration claim, and adopted delivery claim succeed. The minimal fixture REQ has one AC, so “complete” means complete coverage of that isolated parent—not all ACs of the live REQ.
- Retain copied-fixture regressions with structured-blocker assertions: unsafe/symlink paths (`test_validate_rejects_noncanonical_or_malformed_version_one_artifacts`, `test_validate_rejects_symlinked_iteration_qc_path`); malformed schema/config (`test_validate_rejects_noncanonical_or_malformed_version_one_artifacts`, `test_preflight_rejects_wrong_iteration_qc_contract`); duplicate targets (`test_validate_rejects_duplicate_micro_spec_target`); parent/digest/ID/revision and AC errors (`test_validate_rejects_invalid_micro_spec_parent_ac_or_plan`, `test_validate_rejects_stale_adoption_snapshot`); iteration/delivery evidence and authority (`test_validate_rejects_invalid_iteration_qc_evidence`, `test_validate_rejects_invalid_delivery_qc_aggregation`); and pre-adoption absence (`test_claim_requires_explicit_adoption`).
- Keep fixtures isolated from the repository's live empty adoption registry and manual-bootstrap records.
- Keep documentation and skill language explicit that manual QC, iteration-QC, and delivery-QC do not authorize merge, delivery, release, or provenance.

## Non-goals

- Do not add the live `req-0001-r1` snapshot to `specbound.yaml` adoption registry.
- Do not issue, migrate, relabel, or treat manual-bootstrap records as canonical Micro-SPEC, iteration-QC, or delivery-QC evidence.
- Do not implement approval, delivery decision, merge, release, external signing, or live Hermes-profile rollout.
- Do not alter the approved REQ bytes or its approval binding.

## Baseline

AC-001 through AC-005 provide topology/schema, semantic validation, QC aggregation, and an explicit claim-scoped adoption boundary. Existing copied-fixture regressions cover individual accepted/rejected artifact families, but AC-006 needs a single executable proof that the complete adopted evidence chain succeeds through the real CLI while the negative matrix remains fail closed.

## Verification plan

Run from the repository root using memory-backed pytest temporary paths because the host root filesystem has no free space:

```bash
rm -rf /dev/shm/specbound-pytest
.venv/bin/python -m pytest -q tests/test_artifact_topology.py --basetemp=/dev/shm/specbound-pytest
rm -rf /dev/shm/specbound-pytest
.venv/bin/python -m pytest -q --basetemp=/dev/shm/specbound-pytest
rm -rf /dev/shm/specbound-pytest
.venv/bin/python -m specbound.cli preflight
.venv/bin/python -m specbound.cli validate
.venv/bin/python -m specbound.cli validate --claim iteration --requirement req-0001-r1
.venv/bin/python -m specbound.cli validate --claim delivery --requirement req-0001-r1
.venv/bin/python -m compileall -q src
.venv/bin/python -m json.tool schemas/micro-spec.schema.json >/dev/null
.venv/bin/python -m json.tool schemas/iteration-qc.schema.json >/dev/null
.venv/bin/python -m json.tool schemas/delivery-qc.schema.json >/dev/null
git diff --check
```

The last two root claim commands are expected to fail closed with `control_plane_not_adopted`; the isolated happy-path test proves adoption and success without changing the live registry.

## QC exit rule

Record `verified` only after the focused and full suites, copied-fixture real-CLI happy path, negative matrix, root preflight/validation/claim probes, source/schema checks, documentation/skill review, and whitespace checks pass. Otherwise record `not_verified` or `blocked` with the failing command. This manual verdict is not canonical Micro-SPEC, iteration-QC, delivery-QC, an adoption decision, merge authorization, delivery execution, release authorization, or provenance.

## Rollback and containment

This high-risk-parent slice changes only copied-fixture test evidence and this bounded manual-bootstrap record. If a fixture can make an adopted claim pass without canonical validated evidence, retain the reproducer, stop relying on the AC-006 proof, and revert only AC-006-attributable changes. No live adoption, external/runtime action, delivery, merge, or release is authorized.

## Manual QC verdict

**Verified — 2026-07-22 (manual-bootstrap implementation verification only).**

- The focused artifact-topology suite passed using `/dev/shm` pytest temporary storage (the root filesystem has no free space): `.venv/bin/python -m pytest -q tests/test_artifact_topology.py --basetemp=/dev/shm/specbound-pytest` exited 0.
- The full repository suite passed with the same isolated temporary-storage strategy: `.venv/bin/python -m pytest -q --basetemp=/dev/shm/specbound-pytest` exited 0.
- The added copied-fixture real-CLI chain used fixture setup to write canonical Micro-SPEC/iteration-QC/delivery-QC files for the exact one-AC fixture REQ snapshot. The CLI then passed `preflight`, root `validate`, iteration claim, and delivery claim; it validates those files and does not issue them.
- Root `preflight` and normal `validate` returned `valid: true`, exit 0. The live root's iteration and delivery claims each failed as intended with `control_plane_not_adopted`, exit 2; the production registry remains empty.
- `compileall`, all three JSON schema parses, `git diff --check`, and the parent SHA-256 binding check passed.

This is not canonical Micro-SPEC/QC issuance, an adoption decision, approval, merge authorization, delivery/release execution, or provenance.
