---
id: ms-0001-002
kind: manual-bootstrap-micro-spec
status: active
requirement:
  id: req-0001
  revision: 1
  path: .specbound/requirements/req-0001/req-0001-r1.md
  sha256: de286dbc9e12e686df5e628b325020a40206199d78bf36752dbc58754ad47695
selected_acceptance_criteria: [AC-002]
authority: accountable-user-direct-instruction
manual_bootstrap_reason: Canonical Micro-SPEC binding/completeness semantics are not yet adopted; this bounded record is implementation and QC evidence for AC-002 only.
---

# MS-0001-002 — Micro-SPEC parent binding and planning completeness

## 1. Objective

Satisfy **REQ-0001-r1 AC-002** by making a canonical Micro-SPEC a fail-closed, human-readable plan bound to exactly one currently approved REQ snapshot and a non-empty, unique subset of that REQ's acceptance criteria.

## 2. Selected scope

- Extend canonical Micro-SPEC v1 frontmatter validation beyond AC-001's topology/envelope to require exact `id`, `kind: micro-spec`, a single `requirement` mapping (`path`, `id`, `revision`, `sha256`), and `selected_acceptance_criteria`.
- Require the bound REQ to exist at its canonical path, have matching id/revision/frontmatter status `approved`, and have the exact SHA-256 recorded by the Micro-SPEC.
- Extract in-scope AC IDs from the bound REQ and reject empty, duplicate, malformed, or unknown selected IDs.
- Require substantive sections for objective, selected scope, non-goals, baseline, verification plan, QC exit rule, and rollback/containment; high-risk parent REQs require rollback/containment.
- Add valid and copied-fixture negative coverage for parent identity/status/digest mismatch, duplicate/noncanonical target identity, invalid/unknown/duplicate selected ACs, and incomplete planning content.
- Update the Micro-SPEC template, schema, README/governance/skill guidance to state the canonical binding contract and its non-authority boundary.

## 3. Non-goals and boundary

- Do not implement iteration-QC evidence/verdict/coverage (AC-003), delivery-QC aggregation/authority (AC-004), adoption behavior (AC-005), or complete end-to-end adopted evidence (AC-006).
- Do not modify Discovery/REQ approval/rejection semantics, mutate parent REQs, issue approval records, or reinterpret manual-bootstrap records as canonical artifacts.
- Do not treat a canonical Micro-SPEC as approval, iteration QC, delivery QC, merge authorization, delivery decision, or release authority.
- Preserve unrelated existing working-tree changes; modify only AC-002-attributable files.

## 4. Baseline and design decision

AC-001 accepts only canonical topology and `schema_version: 1`; this AC-002 slice adds semantic binding/completeness checks. Reuse safe-path, frontmatter, digest, and REQ validation primitives. Parse only the REQ's explicit `AC-<positive decimal>` list entries; do not infer scope from arbitrary prose. Required headings must have non-empty substantive bodies rather than placeholder-only text.

## 5. Acceptance checks

1. A copied valid fixture with an approved, approval-bound REQ and one canonical Micro-SPEC bound to its current bytes passes validation.
2. A Micro-SPEC with wrong/missing parent path/id/revision, unapproved parent, altered/malformed digest, or `id` that differs from its canonical filename fails closed.
3. Empty, malformed, unknown, or duplicate selected AC sets fail closed; a valid set is a unique subset of currently listed parent REQ AC IDs.
4. Missing or placeholder-only required plan sections fail closed; a high-risk parent cannot omit rollback/containment.
5. Template/schema/documentation/skill use the same binding fields and explicitly preserve the non-authority boundary.

## 6. Verification plan

Run from the repository root:

```bash
.venv/bin/python -m pytest -q tests/test_artifact_topology.py
.venv/bin/python -m pytest -q
.venv/bin/python -m specbound.cli preflight
.venv/bin/python -m specbound.cli validate
.venv/bin/python -m compileall -q src
.venv/bin/python - <<'PY'
from pathlib import Path
required_binding = (
    'requirement:\n  path: .specbound/requirements/req-<id>/req-<id>-r<revision>.md',
    'selected_acceptance_criteria:',
)
for path in ('templates/micro-spec.md', 'README.md', 'docs/governance/issue-sdlc.md', 'skills/specbound-harness/SKILL.md'):
    text = Path(path).read_text(encoding='utf-8')
    assert all(item in text for item in required_binding), path
assert '## QC exit rule' in Path('templates/micro-spec.md').read_text(encoding='utf-8')
PY
git diff --check
```

## 7. Manual QC exit rule

Record `verified` only when focused and full tests, CLI checks, compile, documentation alignment, and diff checks all pass. Otherwise record `not_verified` or `blocked` with the failing command; do not claim AC-002 coverage.

## 8. Rollback and containment

This high-risk-parent slice changes only repository-local validation, fixtures, templates, and guidance. If a false accept/reject or unsafe traversal is found, stop relying on the new binding claim, retain its reproducer, and revert only AC-002-attributable changes. No merge, delivery, release, or external/runtime action is authorized.

## 9. Manual QC verdict

**Verified — 2026-07-22T00:57:53+09:00.** AC-002's bounded parent-binding/planning-completeness scope passed: focused topology tests (`21 passed`), full suite (`67 passed`), `specbound preflight`, `specbound validate`, source compilation, schema JSON parsing, documentation alignment assertions, and `git diff --check`. This manual-bootstrap verdict records implementation/QC evidence only; it does not create canonical iteration-QC/delivery-QC evidence or authorize merge, delivery, or release.
