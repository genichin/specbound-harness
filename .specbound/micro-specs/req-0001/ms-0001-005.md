---
id: ms-0001-005
kind: manual-bootstrap-micro-spec
status: active
requirement:
  id: req-0001
  revision: 1
  path: .specbound/requirements/req-0001/req-0001-r1.md
  sha256: de286dbc9e12e686df5e628b325020a40206199d78bf36752dbc58754ad47695
selected_acceptance_criteria: [AC-005]
authority: accountable-user-direct-instruction
manual_bootstrap_reason: Canonical adoption enforcement has not yet been implemented; this bounded record authorizes only AC-005 policy, validator, fixture, documentation, and skill work.
---

# MS-0001-005 — Control-plane adoption boundary

## Objective

Satisfy **REQ-0001-r1 AC-005** with an explicit, versioned, exact-REQ-snapshot adoption registry and claim-scoped validation: non-adopted REQs remain compatible without canonical Micro-SPEC/QC records, while an adopted REQ cannot make an iteration or delivery claim without its respective canonical evidence.

## Scope

- Define `policy.control_plane_adoption` as a strict version-one configuration contract containing exact `{path, id, revision, sha256}` approved-REQ snapshot entries.
- Validate registry shape, uniqueness, canonical safe paths, exact snapshot identity, current digest, and approved parent status without mutating lifecycle artifacts.
- Add `specbound validate --claim {iteration,delivery} --requirement req-<id>-r<revision>`.
- Require explicit adoption for either claim; require at least one canonical Micro-SPEC and verified canonical iteration-QC for an iteration claim, and canonical delivery-QC for a delivery claim.
- Preserve root `validate` compatibility when required canonical artifacts are absent, including for adopted REQs that make no iteration/delivery claim.
- Keep manual-bootstrap Micro-SPECs non-canonical and unable to satisfy adopted claims.
- Add copied-fixture regression coverage and align template, documentation, and repository skill.

## Non-goals

- Do not issue canonical Micro-SPEC, iteration-QC, or delivery-QC records.
- Do not adopt, migrate, relabel, rewrite, or invalidate existing manual-bootstrap artifacts, REQs, approvals, or Discoveries.
- Do not implement merge authorization, delivery execution, release execution, or release provenance.
- Do not implement AC-006's complete multi-AC adopted end-to-end fixture matrix.

## Baseline

AC-001 through AC-004 validate canonical topology, Micro-SPEC, iteration-QC, and delivery-QC records when present. The repository root contains only manual-bootstrap records; absent canonical artifacts are intentionally compatible before an explicit claim. `specbound validate` currently has no claim selector or adoption registry.

## Verification plan

Run from the repository root:

```bash
.venv/bin/python -m pytest -q tests/test_artifact_topology.py
.venv/bin/python -m pytest -q
.venv/bin/python -m specbound.cli preflight
.venv/bin/python -m specbound.cli validate
.venv/bin/python -m specbound.cli validate --claim iteration --requirement req-0001-r1
.venv/bin/python -m specbound.cli validate --claim delivery --requirement req-0001-r1
.venv/bin/python -m compileall -q src
.venv/bin/python -m json.tool schemas/delivery-qc.schema.json >/dev/null
git diff --check
```

## QC exit rule

Record `verified` only after focused and full suites, normal root validation, claim-shaped fixture probes, preflight, source/schema checks, documentation/skill alignment, and diff checks pass. Otherwise record `not_verified` or `blocked` with the failing command. This manual verdict is not a canonical Micro-SPEC, iteration-QC, delivery-QC, adoption decision, merge authorization, delivery execution, release authorization, or provenance.

## Rollback and containment

This high-risk-parent slice changes only local policy parsing, validator/CLI behavior, copied fixtures, template, documentation, and guidance. If adoption parsing accepts stale or unsafe snapshots, or if a claim is improperly permitted or blocked, stop relying on AC-005 claim results, retain the reproducer, and revert only AC-005-attributable changes. No external/runtime action, delivery, merge, or release is authorized.

## Manual QC verdict

**Verified — 2026-07-22 (manual-bootstrap implementation verification only).**

- `.venv/bin/python -m compileall -q src` exited 0.
- `.venv/bin/python -m pytest -q tests/test_artifact_topology.py` exited 0.
- `.venv/bin/python -m pytest -q` exited 0.
- `.venv/bin/python -m specbound.cli preflight` and normal root `validate` each returned `valid: true`, exit 0.
- Copied-fixture CLI regressions exercised pre-adoption compatibility, explicit adoption, missing iteration/delivery evidence, stale binding, malformed adoption version, and manual-bootstrap exclusion.
- Both canonical QC JSON schemas parsed successfully with `json.tool`; `git diff --check` exited 0.

This is not canonical Micro-SPEC/QC issuance, an adoption decision, approval, merge authorization, delivery/release execution, or provenance. AC-006 remains the separate adopted end-to-end happy-path and full negative-fixture-matrix slice.
