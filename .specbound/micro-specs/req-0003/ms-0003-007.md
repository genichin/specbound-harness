---
schema_version: 1
id: ms-0003-007
kind: micro-spec
requirement:
  path: .specbound/requirements/req-0003/req-0003-r2.md
  id: req-0003
  revision: 2
  sha256: e8e3833e7e1b559bd0ed862cfd13b95a7fd2aadce92b89a9aac8df6b135fc354
selected_acceptance_criteria: [AC-007]
---

# Micro-SPEC — REQ-0003 slice 007: exact publication result

> **Lifecycle boundary:** This artifact binds only AC-007 of exact approved `req-0003-r2`. Implementation requires its own exact digest-bound `approved_for_implementation` review decision. It grants no approval, adoption, implementation-completion, merge, delivery, or release authority.

## Objective

Return the exact structured success result for a completed copied-fixture publication: artifact kind, canonical safe-relative target, canonical identity, and final SHA-256 recomputed from the exact published bytes. A repeat remains a read-only duplicate/conflict failure.

## Scope

- Extend the successful issuance result with canonical identity and final SHA-256 produced by the AC-006 post-write readback.
- Preserve safe-relative canonical target and current family-specific prevalidation.
- Verify reported digest by independently recomputing it from the published fixture leaf.
- Verify repeated requests return duplicate/conflict with target bytes unchanged.
- Keep all mutation limited to the original requested copied-fixture leaf.

## Non-goals

- Status transition, approval/adoption evidence, implementation result, merge, delivery, or release records.
- Idempotent success, content comparison, overwrite, replacement, delete/retry, or reuse of an existing target.
- AC-008 guidance alignment and broader regression/documentation changes.

## Baseline

AC-005 supplies safe exclusive creation; AC-006 fsyncs and recomputes final bytes internally. AC-007 exposes only the exact requested fixture evidence result without adding lifecycle authority.

## Verification plan

- Focused copied-fixture CLI tests assert the success payload includes kind, safe-relative path, identity, and digest matching published bytes.
- Repeat the identical request; assert a duplicate/conflict blocker and unchanged target bytes.
- Run focused/full pytest, preflight, validate, and `git diff --check`.

## QC exit rule

A successful publication has an exact digest-bound structured result; all duplicate paths are non-mutating failures.

## Rollback and containment

No existing target may be altered. Failure and repeat paths are read-only, and all evidence remains confined to explicitly marked copied fixtures.
