---
schema_version: 1
id: ms-0003-009
kind: micro-spec
requirement:
  path: docs/requirements/req-0003/req-0003-r2.md
  id: req-0003
  revision: 2
  sha256: 2c0f5c42a68a7ded6ba8f95fca19cc657986867a928a945a9c969dfba93d28fe
selected_acceptance_criteria: [AC-001, AC-002, AC-003, AC-004, AC-005, AC-006, AC-007, AC-008]
---

# Micro-SPEC — REQ-0003 slice 009: CG-1 fixture-only joint evidence

> **Lifecycle boundary:** This artifact binds the exact approved `req-0003-r2` AC-001 through AC-008 completion group (CG-1) only. It is a planning artifact and requires its own exact digest-bound `approved_for_implementation` review decision before any implementation or fixture artifact publication. It grants no approval, adoption, production publication, implementation-completion, merge, delivery, or release authority.

## Objective

Define the smallest fixture-only, non-authorizing evidence slice that can record CG-1's already-implemented issuance-control verification without mutating the live adoption registry or canonical production artifact roots. The slice must preserve the current validator's one-Micro-SPEC-per-iteration-QC identity contract rather than misrepresenting a cross-slice aggregation as one iteration-QC record.

## Scope

- Recompute and retain exact parent binding for `req-0003-r2` and the exact review-decision bindings for `ms-0003-002` through `ms-0003-008`, covering AC-001 through AC-008.
- Add copied-fixture-only evidence that demonstrates the complete CG-1 command matrix: focused issuance tests, full pytest, preflight, root validation, whitespace check, and live adoption-registry SHA-256 invariance.
- Prove the fixture marker, exact fixture-local adoption prerequisites, safe target derivation, and duplicate/no-mutation behavior before any requested QC artifact is created.
- If canonical QC graph publication is exercised in a copied fixture, preserve the existing contract: an iteration-QC binds exactly one canonical Micro-SPEC and its exact selected AC set; cross-slice aggregation is represented only through existing delivery-QC graph semantics after all required iteration-QC parents exist.
- Record only reproducible command/result/exit-code evidence and explicit fixture paths. No success wording may imply approval, adoption, production readiness, merge, delivery, or release.

## Non-goals

- Changing `specbound.yaml`, live adoption state, approved REQ bytes, approval/review history, or any canonical QC root in the repository.
- Relaxing the iteration-QC one-Micro-SPEC/exact-AC-set validator to publish one synthetic CG-1 iteration-QC over AC-001 through AC-008.
- Publishing a production iteration-QC or delivery-QC, or claiming independent reviewer-agent separation.
- Adding a new artifact family, status transition, adoption, merge, delivery, or release workflow.

## Baseline

`req-0003-r2` is the immutable approved parent bound by SHA-256 `2c0f5c42a68a7ded6ba8f95fca19cc657986867a928a945a9c969dfba93d28fe`. `ms-0003-002` through `ms-0003-008` provide the implemented AC-001 through AC-008 slices and exact digest-bound manual-bootstrap review decisions. The validator currently requires each iteration-QC filename to bind exactly one Micro-SPEC identity and exactly that Micro-SPEC's selected AC set; therefore a single `iqc-0003-009-r1` cannot truthfully aggregate the seven earlier Micro-SPECs. The live adoption registry remains intentionally unchanged.

## Verification plan

- Recompute SHA-256 for the canonical REQ and each bound Micro-SPEC; assert each corresponding review decision binds both exact digests and is `approved_for_implementation`.
- Run `.venv/bin/python -m pytest tests/test_issuance_request.py` and `.venv/bin/python -m pytest`.
- Run `.venv/bin/python -m specbound.cli preflight`, `.venv/bin/python -m specbound.cli validate`, and `git diff --check`.
- In an isolated copied fixture, snapshot live-registry bytes before and after every publication probe; assert byte-for-byte equality and target absence for missing/stale adoption, missing parent, malformed candidate, duplicate, and non-marker cases.
- If fixture QC graph publication is added, exercise a valid per-Micro-SPEC iteration-QC path first, then a delivery-QC edge graph containing exact existing fixture iteration-QC digests; assert a synthetic all-AC iteration-QC is rejected by the existing exact-AC-set rule.

## QC exit rule

The slice is `verified` only when all command evidence passes, every AC has an exact bounded Micro-SPEC/review binding, fixture-only probes preserve live registry bytes, and QC graph assertions do not bypass the one-Micro-SPEC iteration-QC contract. A missing fixture-local adopted parent or graph edge is `blocked` with no target creation. Any proposal to modify live adoption, canonical QC roots, or reviewer independence requires a separate Discovery/REQ decision.

## Rollback and containment

Only source, tests, and disposable copied fixtures may change after a separately approved implementation decision. Failed fixture publication removes only operation-owned fixture output under the existing failure-atomic rules. The canonical approved REQ, review records, live adoption registry, and production artifact roots are immutable and excluded from rollback.
